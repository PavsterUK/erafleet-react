import React from 'react'
import "./RegisterNowBtn.css"

const RegisterNowBtn = (props) => {
  return (
    <button id="register-now-btn" style={{backgroundColor: props.bgColor}} >Register Now</button>
  )
}

export default RegisterNowBtn